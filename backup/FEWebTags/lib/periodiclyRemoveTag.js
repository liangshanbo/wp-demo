/*
 var rmTags = function(type){
 var tags = getTags(type);
 var now = moment().unix();
 for(var i=0,l=tags.length;i<l;i++){
 var version = moment(tags[i],'YYYYMMDDhhmm').unix();
 if(now - version > 24*60*60*config.tags_overdue_num){
 console.log("rm");
 }
 }
 };
 */
